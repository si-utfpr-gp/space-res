<!DOCTYPE html>
<html>
<head>
    <%= render "layouts/shared/head" %>
</head>

<body>
<main>

    <div class="flex items-center justify-center min-h-screen">
        <div class="max-w-sm w-full">

            <div class="px-6 py-4">
                <div class="flex justify-center mb-6">
                    <%= link_to image_tag("avatars/default_avatar.png", class: "w-auto h-24 rounded-xl"), root_path %>
                </div>

                <h2 class="m-3 text-2xl font-medium text-center text-gray-600">
                    <%= yield(:head_title) %>
                </h2>

                <%= render Flash::ToastMessageComponent.new %>

                <%= yield %>
            </div>
        </div>
    </div>

</main>
</body>
</html>
